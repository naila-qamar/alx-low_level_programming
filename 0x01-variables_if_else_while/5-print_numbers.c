#include <stdio.h>

int main(void)
{
    int number;

    /* Loop from 0 to 9 */
    for (number = 0; number < 10; number++)
    {
        /* Print the number */
        printf("%d", number);
    }
    /* Print a new line */
    printf("\n");

    return (0);
}

