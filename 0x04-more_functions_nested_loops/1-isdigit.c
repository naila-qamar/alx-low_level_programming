#include "main.h"

/**
 * _isdigit - Checks for a digit.
 * @c: The character to check.
 *
 * Return: 1 if character is digit, 0 otherwise.
 */
int _isdigit(int c)
{
    return (c >= '0' && c <= '9');
}
